# frozen_string_literal: true

source 'https://rubygems.org'

gem 'google_drive'
gem 'ralyxa', git: 'https://github.com/Dahie/ralyxa.git', branch: 'add-confirmation-status'
gem 'sinatra'

group :development, :test do
  gem 'rerun'
  gem 'rubocop', require: false
  gem 'rubocop-rails'
end
